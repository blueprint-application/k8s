[
  {
    "ref": "#/components/schemas/io.k8s.api.core.v1.ContainerState",
    "schema": {
      "description": "ContainerState holds a possible state of container. Only one of its members may be specified. If none of them is specified, the default one is ContainerStateWaiting.",
      "type": "object",
      "properties": {
        "running": { "$ref": "#/components/schemas/io.k8s.api.core.v1.ContainerStateRunning" },
        "waiting": { "$ref": "#/components/schemas/io.k8s.api.core.v1.ContainerStateWaiting" },
        "terminated": { "$ref": "#/components/schemas/io.k8s.api.core.v1.ContainerStateTerminated" }
      }
    }
  }
]
