[
  {
    "ref": "#/components/schemas/io.k8s.api.networking.v1alpha1.ParentReference",
    "schema": {
      "description": "ParentReference describes a reference to a parent object.",
      "type": "object",
      "properties": {
        "resource": { "description": "Resource is the resource of the object being referenced.", "type": "string" },
        "name": { "description": "Name is the name of the object being referenced.", "type": "string" },
        "namespace": { "description": "Namespace is the namespace of the object being referenced.", "type": "string" },
        "group": { "description": "Group is the group of the object being referenced.", "type": "string" }
      }
    }
  }
]
