[
  {
    "ref": "#/components/schemas/io.k8s.api.core.v1.HostIP",
    "schema": {
      "description": "HostIP represents a single IP address allocated to the host.",
      "type": "object",
      "properties": { "ip": { "description": "IP is the IP address assigned to the host", "type": "string" } }
    }
  }
]
