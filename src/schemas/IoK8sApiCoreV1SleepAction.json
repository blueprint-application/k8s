[
  {
    "ref": "#/components/schemas/io.k8s.api.core.v1.SleepAction",
    "schema": {
      "description": "SleepAction describes a \"sleep\" action.",
      "type": "object",
      "required": ["seconds"],
      "properties": {
        "seconds": { "format": "int64", "description": "Seconds is the number of seconds to sleep.", "type": "integer" }
      }
    }
  }
]
